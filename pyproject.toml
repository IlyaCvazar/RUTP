[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "rutp"
version = "1.0.0"
description = "Reliable UDP Transport Protocol"
requires-python = ">=3.7"

[tool.setuptools.packages.find]
where = ["src"]
